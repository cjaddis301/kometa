services:
  kometa:
    image: kometateam/kometa:v2.5.1
    container_name: kometa

    environment:
      TZ: America/New_York
      KOMETA_TIMES: "03:00"

    env_file:
      - .env

    volumes:
      - ./config:/config:rw

    restart: unless-stopped